'use strict'

module.exports = {
  // Named building asset files with hash in [hash:7] format
  assetHash: true,
  // Named building script files with hash in [chunkhash:7] foramt
  scriptHash: true,
  // Named building style files with hash in [contenthash:7] foramt
  styleHash: true
}
